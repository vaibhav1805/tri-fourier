apiVersion: v1
kind: ServiceAccount
metadata:
  name: autotriage
  namespace: autotriage
  labels:
    app.kubernetes.io/name: autotriage
    app.kubernetes.io/component: rbac
  # For EKS: uncomment and set IAM Role for Service Account (IRSA)
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/autotriage
