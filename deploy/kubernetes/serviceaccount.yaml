apiVersion: v1
kind: ServiceAccount
metadata:
  name: trifourier
  namespace: trifourier
  labels:
    app.kubernetes.io/name: trifourier
    app.kubernetes.io/component: rbac
  # For EKS: uncomment and set IAM Role for Service Account (IRSA)
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/trifourier
