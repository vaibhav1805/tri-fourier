{{- if .Values.rbac.reader.create }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "trifourier.fullname" . }}-reader
  labels:
    {{- include "trifourier.labels" . | nindent 4 }}
rules:
  - apiGroups: ["", "apps", "batch"]
    resources:
      - pods
      - services
      - deployments
      - statefulsets
      - replicasets
      - daemonsets
      - jobs
      - cronjobs
      - nodes
      - events
      - configmaps
      - endpoints
      - namespaces
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources:
      - pods/log
    verbs: ["get"]
  - apiGroups: ["networking.k8s.io"]
    resources:
      - ingresses
      - networkpolicies
    verbs: ["get", "list", "watch"]
  - apiGroups: ["autoscaling"]
    resources:
      - horizontalpodautoscalers
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "trifourier.fullname" . }}-reader-binding
  labels:
    {{- include "trifourier.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "trifourier.fullname" . }}-reader
subjects:
  - kind: ServiceAccount
    name: {{ include "trifourier.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
{{- if .Values.rbac.remediator.create }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "trifourier.fullname" . }}-remediator
  labels:
    {{- include "trifourier.labels" . | nindent 4 }}
rules:
  - apiGroups: ["apps"]
    resources:
      - deployments/scale
      - deployments
    verbs: ["patch", "update", "get"]
  - apiGroups: [""]
    resources:
      - pods
    verbs: ["delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "trifourier.fullname" . }}-remediator-binding
  labels:
    {{- include "trifourier.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "trifourier.fullname" . }}-remediator
subjects:
  - kind: ServiceAccount
    name: {{ include "trifourier.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
