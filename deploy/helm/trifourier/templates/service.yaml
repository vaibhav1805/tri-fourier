apiVersion: v1
kind: Service
metadata:
  name: {{ include "trifourier.fullname" . }}
  labels:
    {{- include "trifourier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "trifourier.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
