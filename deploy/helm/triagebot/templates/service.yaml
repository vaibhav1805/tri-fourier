apiVersion: v1
kind: Service
metadata:
  name: {{ include "autotriage.fullname" . }}
  labels:
    {{- include "autotriage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "autotriage.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
